.content
  - item.images.each_with_index do |i, image|
    = image_tag "#{i.url}" ,data: { index: i }, width: "100", height: '100'
- link_to '編集', edit_item_path(item)
- link_to '削除', item_path(item), method: :delete
%p= item.name
%p
  = item.price
  円
